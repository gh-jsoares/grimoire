name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Run tests
        run: go test ./...

      - name: Build binaries
        run: |
          VERSION=${GITHUB_REF_NAME}
          COMMIT=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          LDFLAGS="-s -w -X main.version=${VERSION} -X main.commit=${COMMIT} -X main.date=${DATE}"

          for TARGET in darwin_arm64 darwin_amd64 linux_arm64 linux_amd64; do
            OS="${TARGET%_*}"
            ARCH="${TARGET#*_}"
            mkdir -p "dist/${TARGET}"
            GOOS=${OS} GOARCH=${ARCH} go build -ldflags "${LDFLAGS}" -o "dist/${TARGET}/grimoire" ./cmd/grimoire
            cp docs/grimoire.1 "dist/${TARGET}/grimoire.1"
            tar -czf "dist/grimoire_${TARGET}.tar.gz" -C "dist/${TARGET}" grimoire grimoire.1
          done

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/grimoire_darwin_arm64.tar.gz
            dist/grimoire_darwin_amd64.tar.gz
            dist/grimoire_linux_arm64.tar.gz
            dist/grimoire_linux_amd64.tar.gz

      - name: Generate changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --tag ${GITHUB_REF_NAME}
        env:
          OUTPUT: CHANGELOG.md

      - name: Commit changelog
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git diff --cached --quiet || git commit -m "docs: update changelog for ${GITHUB_REF_NAME} [skip ci]"
          git push origin HEAD:main

      - name: Update Homebrew tap
        env:
          TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          SHA_ARM64=$(shasum -a 256 dist/grimoire_darwin_arm64.tar.gz | cut -d' ' -f1)
          SHA_AMD64=$(shasum -a 256 dist/grimoire_darwin_amd64.tar.gz | cut -d' ' -f1)

          git clone https://x-access-token:${TAP_TOKEN}@github.com/gh-jsoares/homebrew-tap.git tap
          cd tap

          cat > Formula/grimoire.rb << EOF
          class Grimoire < Formula
            desc "Terminal cheatsheet viewer"
            homepage "https://github.com/gh-jsoares/grimoire"
            version "${VERSION}"
            license "MIT"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/gh-jsoares/grimoire/releases/download/v#{version}/grimoire_darwin_arm64.tar.gz"
                sha256 "${SHA_ARM64}"
              else
                url "https://github.com/gh-jsoares/grimoire/releases/download/v#{version}/grimoire_darwin_amd64.tar.gz"
                sha256 "${SHA_AMD64}"
              end
            end

            def install
              bin.install "grimoire"
              man1.install "grimoire.1"
            end

            test do
              assert_match "grimoire", shell_output("#{bin}/grimoire --version")
            end
          end
          EOF

          sed -i 's/^          //' Formula/grimoire.rb

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/grimoire.rb
          git commit -m "chore: update grimoire to v${VERSION}"
          git push
